---
# The home page. `layout: home` so the custom RusmHome component renders (injected
# via the theme's Layout `home-hero-before` slot). All home content lives in
# .vitepress/theme/RusmHome.vue — do NOT add hero:/features: here (the default
# theme would render its own on top).
layout: home
---
