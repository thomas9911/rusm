[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false

# Project automation, invoked via the `cargo xtask` alias (see .cargo/config.toml).
# Kept dependency-free on purpose: it only orchestrates `bun` and `git`.
[[bin]]
name = "xtask"
path = "src/main.rs"
